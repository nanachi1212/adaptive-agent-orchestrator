# Upstream Attribution

本 repository 是 **fork**，不是原創作品。

## Upstream

| 項目 | 值 |
| --- | --- |
| Upstream URL | https://github.com/irons163/three-tier-agent-orchestrator |
| Upstream author | [irons163](https://github.com/irons163) |
| Upstream repository | `irons163/three-tier-agent-orchestrator` |
| Fork base commit | `ebd723eeb00be8b5b9bffc0594243d7b99c5441f` |
| Fork URL | https://github.com/nanachi1212/adaptive-agent-orchestrator |

## Fork relationship

- 本 repository 透過 GitHub 的 fork 功能建立，GitHub 上保留 `forked from irons163/three-tier-agent-orchestrator` 關係。
- repository 已更名為 `adaptive-agent-orchestrator`，**更名不影響 fork 關係**。
- upstream 的完整 Git history 與原始 commit 作者資訊**予以保留**，不做 rewrite、squash 或 history 移除。
- upstream 被設定為唯讀 remote，本 fork 不向 upstream push。

## Upstream license status

**於驗證時點未觀察到任何明示的 LICENSE。**

| 項目 | 觀察結果 |
| --- | --- |
| 驗證日期 | 2026-09-23 |
| Upstream `LICENSE` 檔 | 不存在 |
| GitHub API `license` 欄位 | `null` |
| GitHub API `allow_forking` | `true` |

因此：

- upstream 所撰寫的內容，其著作權仍屬原作者，**預設保留全部權利**。
- 本 fork **不宣稱** upstream-authored content 已被重新授權。
- 本 repository **刻意不放置 `LICENSE` 檔**，也不將 MIT / Apache-2.0 / GPL 或任何授權條款套用到整個 repository。
- 對 upstream 內容的使用，目前僅依據 GitHub 服務條款所允許的 fork 與檢視範圍。

## 未來需要重新檢查的情況

出現下列任一情況時，必須重新檢視授權狀態，再決定是否調整本檔與 repository 的授權處理：

1. upstream 新增或變更 LICENSE。
2. 取得原作者對本 fork 的額外書面授權。
3. 打算將本 fork 對外散布、發佈到 skill marketplace，或讓第三方安裝使用。
4. 本 fork 對 upstream 原文的改寫程度大到需要重新界定衍生關係。

在上述任一情況完成確認之前，不得新增 LICENSE 檔，也不得移除本檔或 README 中的 upstream 出處標示。

## 本 fork 自行新增的內容

本 fork 新增的檔案（例如 `docs/` 下的文件）由本 fork 作者撰寫。
這不改變 upstream-authored 檔案的著作權歸屬，也不使整個 repository 取得統一授權。
