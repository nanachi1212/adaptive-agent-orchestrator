interface:
  display_name: "Adaptive Agent Orchestrator"
  short_description: "Local-first, capability-based multi-agent orchestration"
  default_prompt: "Use $adaptive-agent-orchestrator to coordinate this task with the lowest sufficient capability, escalating only when justified."
